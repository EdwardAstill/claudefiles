# Skill Tree

Auto-generated from `agentfiles/`. Do not edit by hand — run `python hooks/tools/gen-skill-tree.py` from the repo root.

*73 skills across 6 top-level categories.*

```
agentfiles/
├── coaches/
│   ├── health-advisor     ← Use when the user wants an actionable health protocol (diet, training, supplementation, hormonal / recovery...
│   ├── kb-critic     ← Use when the user wants the personal knowledge base audited adversarially — evidence quality, contradiction...
├── coding/     ← Category dispatcher for coding work.
│   ├── architecture     ← Use when deciding how a system fits together at the service / layer / boundary level.
│   ├── dsa     ← Use when picking a data structure or algorithm, or analyzing complexity.
│   ├── api/
│   │   ├── api-architect     ← Use when designing or reviewing an API contract — the shape of endpoints, request/response payloads, status...
│   ├── ci-cd/
│   │   ├── github-actions-expert     ← Use when writing, debugging, or reviewing GitHub Actions workflows.
│   ├── data/
│   │   ├── database-expert     ← Use for schema design, migrations, query optimization, indexing, and ORM work (SQLAlchemy, Prisma, Diesel).
│   │   ├── file-converter     ← Use when converting a file already on disk (PDF, image, document) into AI-friendly markdown, RAG chunks, a...
│   ├── infrastructure/
│   │   ├── infrastructure-expert     ← Use for Dockerfiles, docker-compose, Kubernetes manifests, Terraform or Pulumi, cloud configuration (AWS/GC...
│   ├── languages/
│   │   ├── python     ← Use when writing, reviewing, or debugging Python with attention to the real project toolchain: pyright LSP...
│   │   ├── rust     ← Use when writing, reviewing, or debugging Rust with attention to ownership, lifetimes, error handling, and...
│   │   ├── storm-tui     ← Use when the TUI is being built specifically with @orchetron/storm — the React-based compositor-driven term...
│   │   ├── tui     ← Use for designing, specifying, or reviewing a terminal user interface (TUI) — what it looks like and how it...
│   │   ├── typescript     ← Use when writing, reviewing, or debugging TypeScript/JavaScript with strict typing, bun/npm tooling, and th...
│   │   ├── typst     ← Use when writing, styling, or debugging Typst documents.
│   │   ├── ui     ← Use for web UI design, layout, or review in the React + Tailwind + shadcn/ui stack — including visual verif...
│   ├── quality/     ← Sub-category dispatcher for code quality workflows.
│   │   ├── accessibility     ← Use when building or reviewing web UI against WCAG 2.1 AA — semantic HTML, ARIA, keyboard navigation, scree...
│   │   ├── code-review     ← Use when either requesting a code review of completed work or acting on review feedback you've received.
│   │   ├── dependency-management     ← Use for bumping dependency versions, analyzing breaking changes, CVE remediation, lock-file hygiene, and au...
│   │   ├── documentation     ← Use when the primary output is documentation prose — READMEs, API docs, architecture guides, ADRs, changelo...
│   │   ├── observability     ← Use for structured logging, distributed tracing (OpenTelemetry), metrics instrumentation, and alerting rule...
│   │   ├── performance-profiling     ← Use when code is correct but too slow or memory-hungry, and you need to find the real bottleneck with measu...
│   │   ├── refactoring-patterns     ← Use for deliberate large-scale restructuring of existing code — not quick cleanup, but a multi-step transfo...
│   │   ├── regex-expert     ← Use for mass search-and-replace or regex-driven text manipulation across a codebase, using `sd` and `ripgre...
│   │   ├── repomix     ← Use when packing a whole repo or directory into a single AI-friendly file for LLM context.
│   │   ├── security-review     ← Use when scanning code for security vulnerabilities, hardening an app before production, or auditing a spec...
│   │   ├── simplify     ← Use after an implementation is working and tests pass, to clean up recently changed code without changing b...
│   │   ├── skill-tester     ← Use when measuring whether an agentfiles skill actually adds value — running it against hard benchmark ques...
│   │   ├── systematic-debugging     ← Use when something is broken and the root cause isn't known yet — bugs, test failures, unexpected behavior,...
│   │   ├── tdd     ← Use before writing any production code — every new feature, bug fix, refactor, or behavior change starts wi...
│   │   ├── verification-before-completion     ← Use right before claiming any work is complete, fixed, or passing — mandatory gate before committing, openi...
│   ├── version-control/
│   │   ├── git-expert     ← Use for local git operations — branching, worktrees, merging, diffing, history, and repo state assessment.
│   │   ├── git-worktree-workflow     ← Use for the full git-worktree lifecycle — starting isolated feature work in a new worktree and, later, merg...
│   │   ├── github-expert     ← Use for any GitHub-hosted operation via the `gh` CLI — PRs, issues, releases, Actions status, browsing/clon...
├── management/     ← Category dispatcher for orchestration and skill-system tooling.
│   ├── consultants/
│   │   ├── coordination-advisor     ← Use when manager is deciding how to structure agent teams for a multi-agent plan.
│   │   ├── design-advisor     ← Use when manager is deciding whether a task needs brainstorming or a written plan before implementation beg...
│   │   ├── git-advisor     ← Use when manager is deciding git strategy for multi-agent work.
│   ├── meta/     ← Sub-category dispatcher for skill-system tooling.
│   │   ├── agentfiles-manager     ← Use when setting up or modifying which skills are installed for a project.
│   │   ├── audit     ← Use when verifying the agentfiles manifest is consistent with what is on disk.
│   │   ├── documentation-maintainer     ← Use when repo documentation needs to be synced with the actual skill hierarchy, CLI surface, or manifest af...
│   │   ├── retrospective     ← Use when analysing session logs to surface routing failures, self-loops, wasted skill loads, and recovery p...
│   │   ├── skill-catalog     ← Use when the user wants to browse the full list of available agentfiles skills.
│   │   ├── using-agentfiles     ← Use at session start as the primer that establishes how skills work, the default routing rule (executor for...
│   │   ├── writing-skills     ← Use when authoring or modifying a SKILL.md.
│   ├── orchestration/
│   │   ├── executor     ← Use at the start of every new task before any other skill.
│   │   ├── manager     ← Use only when work genuinely needs parallel multi-domain agents.
│   │   ├── subagent-driven-development     ← Use when an implementation plan or spec already exists and the tasks are mostly independent.
├── planning/     ← Use when the user is somewhere in the design → plan → execute lifecycle but unsure which phase-skill to pick.
│   ├── brainstorming     ← Use when starting work on a feature or change where requirements and approach are not fully defined — BEFOR...
│   ├── writing-plans     ← Use when an approved spec / requirements doc exists and needs to become a bite-sized, TDD-structured implem...
├── reasoning/     ← Use when the user asks a formal problem-solving question that dispatches to a specialist: designing a game,...
│   ├── constraint-solver     ← Use when the user describes a declarative "find X such that Y" problem — scheduling, assignment, packing, t...
│   ├── game-designer     ← Use when turning a rough game concept into an implementable systems-design doc (pillars, core loop, MDA, pr...
│   ├── logic-puzzle-designer     ← Use when generating uniquely-solvable logic puzzles at a target difficulty — Sudoku, nonograms, Kakuro, Nur...
├── research/     ← Category dispatcher for research tasks.
│   ├── browser-control     ← Use when driving a real browser session.
│   ├── codebase-explainer     ← Use when building a mental model of an unfamiliar repo.
│   ├── codebase-to-course     ← Use when turning a codebase into a self-contained interactive HTML course that teaches non-engineers (vibe...
│   ├── computer-control     ← Use when driving the user's Linux/Hyprland desktop directly — not a browser, not a terminal scrollback.
│   ├── docs-agent     ← Use when the user needs exact API reference, config options, or a working code example for a specific libra...
│   ├── github-repo-researcher     ← Investigate GitHub repos — search for repos by topic, analyze how a repo works, break down its architecture...
│   ├── knowledge-base     ← Use BEFORE answering any question touching health, training, enhancement, pharmacology, nutrition, exercise...
│   ├── readrun     ← Use when creating or editing readrun documentation — folders of Markdown rendered by the `rr` CLI as intera...
│   ├── research-agent     ← Use when the user needs a trade-off, risk, or evidence analysis before committing.
│   ├── sci-hub     ← Use when the user wants to obtain the full PDF of an academic paper, journal article, or preprint identifie...
│   ├── terminal-read     ← Use when the user references output that happened in their shell outside the conversation and hasn't pasted...
│   ├── test-taker     ← Use when the user hands over a question set plus reference material and wants every question answered in on...
│   ├── web-scraper     ← Use for pure-HTTP scraping when data is reachable without a real browser.
│   ├── youtube     ← Use for anything YouTube.
```
